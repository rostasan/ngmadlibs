/* 
This controller is used to display 
error messages during form entry and validation.
 */
(function(){
    angular
            .module('madApp')
            .controller('formMessage', formMessage);
    function formMessage(){

    };
});